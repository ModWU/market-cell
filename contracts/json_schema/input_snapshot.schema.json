{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://marketcell.local/schemas/input_snapshot.schema.json",
  "title": "MarketCell InputSnapshot",
  "type": "object",
  "required": [
    "snapshot_id",
    "input_kind",
    "target",
    "horizon",
    "content_hash",
    "data_version",
    "source",
    "payload",
    "payload_size_bytes",
    "created_at",
    "schema_version",
    "metadata"
  ],
  "additionalProperties": false,
  "properties": {
    "snapshot_id": {"type": "string", "minLength": 1},
    "input_kind": {
      "enum": ["analysis_request", "candle_batch", "feature_snapshot"]
    },
    "target": {"type": "string", "minLength": 1},
    "horizon": {"type": "string", "minLength": 1},
    "content_hash": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "data_version": {"type": "string", "minLength": 1},
    "source": {"type": "string", "minLength": 1},
    "payload": {"type": "object", "additionalProperties": true},
    "payload_size_bytes": {"type": "integer", "minimum": 2},
    "created_at": {"type": "string", "minLength": 1},
    "schema_version": {"const": "input_snapshot.v1"},
    "metadata": {"type": "object", "additionalProperties": true}
  }
}
